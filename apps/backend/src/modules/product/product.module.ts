import { Module } from '@nestjs/common';

/**
 * Product Module (stub).
 * Will be fully implemented in P04 (Product Management).
 * Handles: products, variants, images, categories, pricing, sizes, FAQs.
 */
@Module({})
export class ProductModule {}
