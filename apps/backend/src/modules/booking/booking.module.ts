import { Module } from '@nestjs/common';

/**
 * Booking Module (stub).
 * Will be fully implemented in P07 (Booking & Availability Engine).
 * Handles: bookings, availability, date blocks, cart validation.
 */
@Module({})
export class BookingModule {}
