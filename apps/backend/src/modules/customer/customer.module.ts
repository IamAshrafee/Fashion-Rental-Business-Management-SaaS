import { Module } from '@nestjs/common';

/**
 * Customer Module (stub).
 * Will be fully implemented in P05 (Customer Management).
 * Handles: customer CRUD, booking history, stats.
 */
@Module({})
export class CustomerModule {}
