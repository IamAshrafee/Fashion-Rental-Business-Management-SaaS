import { Module } from '@nestjs/common';

/**
 * Payment Module (stub).
 * Will be fully implemented in P08 (Payment & Deposit System).
 * Handles: payment recording, deposits, refunds, damage claims.
 */
@Module({})
export class PaymentModule {}
