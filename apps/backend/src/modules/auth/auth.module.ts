import { Module } from '@nestjs/common';

/**
 * Auth Module (stub).
 * Will be fully implemented in P03 (Auth & Tenant System).
 * Handles: login, register, JWT tokens, refresh tokens, sessions.
 */
@Module({})
export class AuthModule {}
