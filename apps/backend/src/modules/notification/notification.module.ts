import { Module } from '@nestjs/common';

/**
 * Notification Module (stub).
 * Will be fully implemented in P10 (Notifications & Background Jobs).
 * Handles: SMS, email, in-app notifications, event listeners.
 */
@Module({})
export class NotificationModule {}
