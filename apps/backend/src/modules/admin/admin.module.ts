import { Module } from '@nestjs/common';

/**
 * Admin Module (stub).
 * Will be fully implemented in P19 (SaaS Admin Portal).
 * Handles: tenant management, subscription plans, system settings, analytics.
 */
@Module({})
export class AdminModule {}
