/**
 * Re-export useAuth from the auth provider.
 * This file exists so consumers can import from '@/hooks/use-auth'
 * following the project's hook convention.
 */
export { useAuth } from '@/providers/auth-provider';
